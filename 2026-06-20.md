# 2026-06-20 — HPC Exercise 1

## Plan

1. **Orfeo status check** — Connect (`wsl` → `orfeo`), run `squeue -u $USER` and `sacct -u $USER --starttime=2026-06-11 --format=JobID,JobName,Partition,State,ExitCode,Elapsed`. Note the state of the four benchmark jobs (1277489–1277506).
2. **Unblock THIN benchmarks** — If THIN jobs are still pending, check partition health with `sinfo -p THIN`. Cancel stale jobs if needed (`scancel <jobid>`), then resubmit `bcast_thin.sh` and `barrier_thin.sh`. Goal: `results/bcastTHIN.csv` and `results/barrierTHIN.csv`.
3. **Fix and re-run EPYC barrier** — `barrierEPYC.csv` incomplete. Keep `#SBATCH --time=02:00:00` (Orfeo max). Reduce workload (`iterations=10`, lower `-i`/`-x`), then resubmit `barrier_latencies.sh`.
4. **Confirm EPYC bcast is usable** — `bcastEPYC.csv` has good coverage (7309 lines, all algos to 256 procs). Spot-check line counts per algorithm/processor before moving on.
5. **Validate the full dataset** — Once all jobs finish, run a quick coverage check on all four CSVs (algorithms present, processor counts, repetition counts). Flag any gaps.
6. **Sync results locally** — Copy CSVs from Orfeo into `exercise1/results/`, commit, and push.
7. **Start analysis** — Create `exercise1/scripts/results_and_modelling.ipynb`: log-log plots (latency vs processors, latency vs message size) and draft performance-model equations for the report.

## Execution log

- **Orfeo state check.** Verified scripts and result files after break. `barrierEPYC.csv` missing; Orfeo script still had invalid 4h limit. `bcastEPYC.csv` present.
- **EPYC barrier script fix.** Reverted to `02:00:00`, set `iterations=10`, `-i/-x 500`. Submitted job **1303630**.
- **EPYC bcast validated.** 7309 lines; all algos (0,1,2,5) reach 256 procs. Marked analysis-ready.
- **Job 1303630 monitored.** Ran ~1h38m; hit **TIMEOUT** at 2h with **335 lines** (~70% of target).
- **Split-job strategy.** Added `barrier_latencies_alg01.sh` + `alg24.sh`. Resolved two `git pull` conflicts (discard Orfeo edits, pull from GitHub).
- **Job 1303667 (alg01, 1h).** **TIMEOUT** at 1h with **168 lines** (7/10 iterations). Bumped split scripts to `02:00:00`.
- **Job 1303691 (alg01, 2h).** **COMPLETED** in 1h26m. `barrierEPYC_alg01.csv`: 240 lines; algo 0=120, algo 1=119 rows.
- **Job 1303806 (alg24, 2h).** **COMPLETED** in 1h27m. `barrierEPYC_alg24.csv`: 241 lines; algo 2=120, algo 4=120 rows.

- **Merged `barrierEPYC.csv`.** 480 lines from alg01 + alg24. **All four benchmark CSVs complete.**

- **Full dataset validated.** All four CSVs confirmed. Exercise 1 data collection **complete**.

- **Synced CSVs locally** via WSL `scp` (4 files in `exercise1/results/`). Fixed WSL `~/.ssh/config` (`orfeo_key_new`).
- **Committed & pushed** to GitHub (`cde300e`). Data phase **complete**.

**Still open:** analysis notebook → report & slides.

