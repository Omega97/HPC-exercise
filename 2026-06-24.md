# 2026-06-24 — HPC Exercise 2c (Mandelbrot, OpenMP scaling)

## The Plan for Today

Carry forward from 2026-06-23: strong scaling OMP on EPYC is done (job **1309811**, 65-line CSV). Weak scaling was submitted as job **1309830** — verify it, sync data locally, then move toward plots and report.

1. **Check weak scaling job 1309830** — `sacct` for state/exit code; `wc -l` and `head` on `weak_scaling_OMP_EPYC.csv` (expect **12 lines**: header + 11 runs, threads 1→64, `C=1e6`).
2. **Validate weak-scaling data** — confirm walltimes are sensible; flag any failed runs or missing rows; re-submit only if needed.
3. **Sync Orfeo results locally** — copy `strong_scaling_OMP_EPYC.csv` and `weak_scaling_OMP_EPYC.csv` into `exercise2/results/`.
4. **Plots** — strong scaling: speedup & efficiency vs threads; weak scaling: walltime vs threads (and ideal constant-time reference). Save under `exercise2/plots/`.
5. **Commit & push** — CSVs, plots, and any script tweaks; keep Orfeo repo synced (`git pull` before next job).
6. **Report draft (if time)** — ex2 method, OMP scheduling/bind settings, scaling results, brief conclusions.

**Target data files (Orfeo → `exercise2/results/`):**

| File | Status |
|---|---|
| `strong_scaling_OMP_EPYC.csv` | Synced locally (65 lines) |
| `weak_scaling_OMP_EPYC.csv` | Synced locally (12 lines) |
| `../plots/mandelbrot_set.pgm` | Done (smoke test 1308944) |

**Context from yesterday (strong scaling, job 1309811):**

| Threads | Walltime (s) | Speedup vs 1 | Efficiency |
|---:|---:|---:|---:|
| 1 | 211.24 | 1.0 | 100% |
| 8 | 45.92 | 4.6 | 57% |
| 16 | 24.80 | 8.5 | 53% |
| 32 | 12.42 | 17.0 | 53% |
| 64 | 6.60 | **32.0** | **50%** |

---

## Execution Log

- **Session start.** Created daily note; strong scaling confirmed OK on Orfeo (1309811). Weak scaling job **1309830** submitted at end of yesterday — results pending (user will paste into `ai-feed.md`).
- **Weak scaling — job 1309830 COMPLETED** (1h 6m 27s, exit 0). `weak_scaling_OMP_EPYC.csv` has **12 lines** (header + 11 runs). Early rows look OK (1→2 threads: ~211 s constant); from 4 threads walltime jumps (~361 s at 4 threads, ~367 s at 8) — worth plotting full CSV; likely memory/cache pressure at larger image sizes, not a script failure.
- **Synced CSVs locally** via WSL/`orfeo_key_new`. Verified: `strong_scaling_OMP_EPYC.csv` (64 thread counts, T₁=211.24 s, T₆₄=6.60 s); `weak_scaling_OMP_EPYC.csv` (11 runs, flat ~211 s at 1–2 threads, rises to ~416 s at 64 threads / 8000×8000).
- **Plots generated.** `exercise2/scripts/plot_scaling.py` → `plots/strong_scaling_OMP_EPYC.png` (walltime, speedup, efficiency) and `plots/weak_scaling_OMP_EPYC.png` (walltime vs ideal, problem size).