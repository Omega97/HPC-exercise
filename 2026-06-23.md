# 2026-06-23 — HPC Exercise 2c (Mandelbrot, OpenMP-only)

## The Plan for Today

Exercise 2c only. Refactor the hybrid prototype in `exercise2/` to pure OpenMP, then prepare for Orfeo scaling runs. You submit jobs; results are logged below.

1. **Refactor `exercise2/apps/main.c`** — remove MPI (`MPI_Init_thread`, row split, `MPI_Gatherv`); match spec CLI `./mandelbrot n_x n_y x_L y_L x_R y_R I_max`; allocate full image; time with `omp_get_wtime()`; always write PGM; print `Total Walltime (s): …` for scripts.
2. **Update build** — OpenMP-only `CMakeLists.txt` and `exercise2/scripts/make.sh` (no `mpicc` / MPI).
3. **Local smoke test** — build and run a small image (e.g. 200×200); confirm `mandelbrot_set.pgm` looks correct.
4. **Fix OMP scaling scripts** — `strong_scaling_OMP_EPYC.sh`, `weak_scaling_OMP_EPYC.sh`: `OMP_NUM_THREADS`, new CLI (no `threads` arg, no `mpirun`); fix CSV headers; add `smoke_test.sh`.
5. **Orfeo smoke test** — submit `smoke_test.sh`; verify PGM and timing on cluster.
6. **Orfeo strong scaling** — submit `strong_scaling_OMP_EPYC.sh`; sync `strong_scaling_OMP_EPYC.csv`.
7. **Orfeo weak scaling** — submit `weak_scaling_OMP_EPYC.sh`; sync `weak_scaling_OMP_EPYC.csv`.
8. **Plots + report notes** — speedup/efficiency from CSVs; draft report sections if time allows.

**Target data files (Orfeo → `exercise2/results/`):**

| File | Content |
|---|---|
| `strong_scaling_OMP_EPYC.csv` | `Threads,Size,Walltime(s)` — fixed 1000×1000, threads 1–64 |
| `weak_scaling_OMP_EPYC.csv` | `Threads,Size,Walltime(s)` — constant work/thread, `C=1e6` |
| `../plots/mandelbrot_set.pgm` | Reference image from smoke test |

---

## Execution Log

- **Refactored `exercise2/apps/main.c`.** Removed MPI (init, row split, `MPI_Gatherv`). CLI now matches spec: `./mandelbrot n_x n_y x_L y_L x_R y_R I_max [output.pgm]`. Threads controlled via `OMP_NUM_THREADS`. Always writes PGM; prints `Total Walltime (s): …`.
- **Updated build.** `apps/CMakeLists.txt` no longer links MPI; `make.sh -c` uses `gcc -fopenmp` instead of `mpicc`.
- **Local smoke test (WSL).** `OMP_NUM_THREADS=4 ./mandelbrot 200 200 -2.0 -1.0 0.5 1.0 255 smoke.pgm` → walltime **0.005 s**, PGM header `P5 / 200 200 / 255`, file **40 KB**. Builds OK: `exercise2/bin/mandelbrot` (gcc) and `exercise2/build/bin/mandelbrot` (CMake).
- **Fixed OMP scaling scripts.** `strong_scaling_OMP_EPYC.sh`, `weak_scaling_OMP_EPYC.sh`: no `mpirun`, `OMP_NUM_THREADS` from loop, spec CLI, CSV truncated with `>`, image written to `/dev/null` during benchmarks. Added `smoke_test.sh` (200×200 → `plots/mandelbrot_set.pgm`).
- **Orfeo `git pull` fix.** Untracked `exercise1/results/{bcast,barrier}*.csv` on Orfeo blocked merge (same files now tracked on GitHub). Fix: `rm` those four files, then `git pull`. Pulled `11e137c..0801ccd`, then pushed local ex2 work (`25f8c0c`) and pulled on Orfeo.